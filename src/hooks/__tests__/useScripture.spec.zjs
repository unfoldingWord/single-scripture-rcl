import { isOriginalBible } from '../../../dist/utils/ScriptureSettings'

describe('test', () => {
  test('test', () => {
    const results = isOriginalBible('tit')
    expect(results).toBeFalsy()
  })
})
